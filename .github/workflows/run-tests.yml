name: Run Behave Tests

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y wget unzip

      - name: Install Chrome
        run: |
          wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          sudo sh -c 'echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google-chrome.list'
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable

      - name: Install Python dependencies with compatible versions
        run: |
          python -m pip install --upgrade pip
          pip install behave==1.2.6 selenium webdriver-manager
          pip install behave-webdriver || echo "behave-webdriver installation may have warnings"

      - name: Verify installations
        run: |
          python -c "import behave; print('behave version:', behave.__version__)"
          python -c "import selenium; print('selenium version:', selenium.__version__)"
          google-chrome --version

      - name: Run candymapper behave tests
        run: |
          cd candymapper
          behave --no-capture --format plain

      - name: Run peppers-ghost behave tests
        run: |
          cd peppers-ghost
          behave --no-capture --format plain

      - name: Upload candymapper screenshots on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: candymapper-failure-screenshots
          path: candymapper/screenshots/
          if-no-files-found: ignore

      - name: Upload peppers-ghost screenshots on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: peppers-ghost-failure-screenshots
          path: peppers-ghost/screenshots/
          if-no-files-found: ignore

      - name: Upload root screenshots on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: root-failure-screenshots
          path: screenshots/
          if-no-files-found: ignore
